C# is just one of the programming languages, that the .NET Framework Supports.
.NET's main components are the large library and the common language runtime (CLR)
When the C# code is compiled, it goes through the common intermediate language (CIL) and the CLR before it becomes machine code.